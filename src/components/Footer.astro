<footer class="footer">
  <div class="inner">
    <span>© {new Date().getFullYear()} Matglitch</span>
    <a href="#top">Retour en haut ↑</a>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--border);
    color: var(--fg-dim);
    font-family: var(--mono);
    font-size: 0.8rem;
  }

  .inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 1.75rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
  }

  a { color: var(--fg-dim); }
  a:hover { color: var(--accent); text-decoration: none; }
</style>
